# GUI + Standalone-Bauen. bench.py selbst braucht KEINE Pakete (reine stdlib).
# Installation (empfohlen im venv):
#   python3 -m venv .venv
#   source .venv/bin/activate.fish      # bzw. .venv/bin/activate / .venv\Scripts\activate
#   pip install -r requirements.txt
PySide6>=6.6
pyinstaller>=6.0
